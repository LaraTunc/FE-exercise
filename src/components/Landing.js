import React from 'react';

const Landing = () => {
	return (
		<div className="landing">
			<div className="hero" />
			<p className="landing-title">Patio Furniture</p>
			<button className="button padded">SHOP</button>
		</div>
	);
};

export default Landing;
